POOL='root'

# zfs file system to mount/unmount on sign-in/sign-out
ZHOME="$POOL/$USER"

# array of usernames that use homelock
# (bash array syntax -- space separated, no commas)
USERS=()

# KMSCONMSG_TIMEOUT='10' # seconds
# POWERSAVE_TIMEOUT='1' # minutes
